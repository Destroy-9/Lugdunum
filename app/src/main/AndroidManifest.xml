<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lugdunum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lugdunum">
        <activity android:name=".HistoryActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".SettingsActivity"
            android:parentActivityName=".ChoiceJourneyActivity" />
        <activity android:name=".games.ChairAnswerActivity" />
        <activity android:name=".games.ChairActivity" />
        <activity android:name=".games.BridgeAnswerActivity" />
        <activity android:name=".games.BridgeActivity" />
        <activity android:name=".games.CrayonActivity" />
        <activity android:name=".games.CuriosityGameActivity" />
        <activity android:name=".games.TheatreGameActivity" />
        <activity android:name=".ChoiceGameActivity" />
        <activity android:name=".games.FourviereGameActivity" />
        <activity
            android:name=".games.pop"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".RulesActivity"
            android:parentActivityName=".SettingsActivity" />
        <activity
            android:name=".ChoiceJourneyActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".games.pop1"
            android:theme="@style/CustomTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDrPfHFCl1t0oY7BKbC1sKib5ZXSNU7fKY" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>